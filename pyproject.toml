[tool.poetry]
name = "gaftools"
version = "0.1.0"
description = ""
authors = ["qinqian <qqin@broadinstitute.org>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
rich-click = "^1.6.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
gaftools = "gaftools.cli:cli"